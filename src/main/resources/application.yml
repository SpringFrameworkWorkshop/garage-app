spring:
  datasource:
    url: jdbc:h2:mem:garage;MODE=MySQL
    username: a
    password: a
    driver-class-name: org.h2.Driver

  data:
    rest:
      basePath: /

---
spring:
  profiles: prod

  datasource:
    url: jdbc:mysql://localhost:3306/garage?createDatabaseIfNotExist=true&useSSL=false&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: root
    password: pablo-1
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop